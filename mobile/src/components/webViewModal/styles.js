import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  loadingContainer: {
    position: 'absolute',
    width: '100%',
    paddingTop: 50,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
